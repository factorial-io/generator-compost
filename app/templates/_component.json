{
  "name": "<%= props.name %>",
  "repository": "factorial-io/<%= props.name %>",
  "version": "0.0.0",
  "styles": [<% if (props.addStyles) { %>"<%= props.name %>.css"<% } %>],
  "scripts": [<% if (props.addScripts) { %>"<%= props.name %>.js"<% } %>],
  "templates": [<% if (props.addTemplate) { %>"<%= props.name %>.<%= props.templateName %>"<% } %>],
  "dependencies": {<% if (props.addScripts) { %>
    "components/jquery": "*",
    "components/jqueryui": "*"<% } %>
  },
  "license": "MIT"<% if (props.addTemplate) { %>,
  "backend": {
    "template": "<%= props.name %>.<%= props.templateName %>",
    "fixture": {},
    "options": {}
  }<% } %>
}
